module.exports = {
    plugins: [
        function() {
            const compiler = this; 

            compiler.plugin("compilation", (compilation) => {
                debugger;                
            })
        }
    ]
}